# Runtime requirements bundled into the AppImage.
# Versions must stay in sync with pyproject.toml [project.dependencies] (the
# wheel staged by scripts/build_appimage.sh already carries these as its own
# Requires-Dist; listed again here for a human reading this file). PySide6
# payload trim carried over from the MSI payload trim (4e280ae,
# "fix(windows): make the MSI installable..."; reference Linux cx_Freeze
# freeze 608 MB -> 163 MB):
#   - PySide6-Essentials instead of PySide6: the client only imports
#     QtCore/QtGui/QtWidgets (grep-verified) plus the QtSvg image-format
#     plugin for icon rendering, all shipped in Essentials. The full PySide6
#     meta-package pulls in PySide6-Addons too (WebEngine, Quick3D, Charts,
#     Multimedia, 3D...) for none of which this client has any use --
#     hundreds of MB dead weight in every AppImage.
#   - grpcio-tools dropped: it ships protoc, a build-time compiler. The gRPC
#     stubs under saebooks_desktop/grpc_gen/ are generated and committed, so
#     protoc has no business in a shipped client (~40 MB of dev tooling).
#     protobuf itself stays: the generated stubs `import google.protobuf`
#     directly at runtime, so it must remain a real (non-dev) dependency even
#     though grpcio-tools -- which used to drag it in transitively -- is gone.
PySide6-Essentials>=6.5
httpx>=0.27
platformdirs>=4.0
grpcio>=1.60
protobuf>=4.0
