# Runtime requirements bundled into the AppImage.
# Versions must stay in sync with pyproject.toml [project.dependencies].
# scripts/build_appimage.sh appends a freshly-built saebooks_desktop wheel to
# this list (by absolute path) at staging time — do not list saebooks-desktop
# here.
PySide6>=6.5
httpx>=0.27
platformdirs>=4.0
grpcio>=1.60
grpcio-tools>=1.60
